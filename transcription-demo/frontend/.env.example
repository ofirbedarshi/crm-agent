# Hello — transcription demo frontend (Vite).
# Copy to `transcription-demo/frontend/.env` or `.env.local` if you need overrides.

# Optional. Demo API base URL (default http://localhost:4001).
# VITE_API_BASE=http://localhost:4001
